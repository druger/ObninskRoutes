<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.example.druger.obninskroutes" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" >
    </uses-feature>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ru.example.druger.obninskroutes.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="ru.example.druger.obninskroutes.ListStops"
            android:label="@string/title_activity_list_of_stops"
            android:parentActivityName="ru.example.druger.obninskroutes.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.example.druger.obninskroutes.MainActivity" />
        </activity>

        <activity
            android:name="ru.example.druger.obninskroutes.timetable.Timetable"
            android:label="@string/title_activity_timetable"
            android:parentActivityName="ru.example.druger.obninskroutes.ListStops" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.example.druger.obninskroutes.ListStops" />
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="ru.example.druger.obninskroutes.MapsActivity"
            android:label="@string/title_activity_maps" >
        </activity>
        <activity
            android:name="ru.example.druger.obninskroutes.GooglePlayServicesActivity"
            android:label="@string/title_activity_google_play_services" >
        </activity>

        <activity
            android:name="ru.example.druger.obninskroutes.search.SearchableActivity"
            android:label="@string/title_activity_searchable" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" >
            </meta-data>
        </activity>

        <activity
            android:name="ru.example.druger.obninskroutes.search.RecordActivity"
            android:label="@string/title_activity_record" >
        </activity>
        
        <provider
            android:authorities="ru.example.druger.obninskroutes.search.SuggestionProvider"
            android:name="ru.example.druger.obninskroutes.search.SuggestionProvider"
            android:exported="false"/>

        <meta-data
            android:name="android.app.default_searchable"
            android:value=".search.SearchableActivity" />
    </application>

</manifest>
